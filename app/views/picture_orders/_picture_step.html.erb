  <%= @calc.first_step %>
  </br>
  <%= f.input :human_count, collection: @human_counts, include_blank: false %>
  <%= f.input :size, collection: @sizes, :include_blank => false %>
  <%= f.input :frame, collection: @frames, :include_blank => false %>

  <%= f.association :picture_user_photos, as: :file %>

  <%= f.simple_fields_for :picture do |c| %>
  <%= c.input :photo_picture_id, collection: PhotoPicture.all, include_blank: false %>
  <%= c.grouped_collection_select :id, PhotoPicture.order(:name), :pictures, :name,
								:id, :name %>
  <% end %>
  <%= f.input :gel %>
  <%= f.input :comments, :as => :text, :input_html => { rows: 5 } %>
